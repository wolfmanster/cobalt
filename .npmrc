# Keep the dependency tree inside this project and avoid Windows workspace
# symlink permission issues during installation.
node-linker=hoisted
store-dir=.pnpm-store
cache-dir=.pnpm-cache
state-dir=.pnpm-state
devdir=.cache/node-gyp
